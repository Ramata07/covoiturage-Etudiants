/*

Corriger l'implémentation des fichiers (enums, relations, les fichiers index manquants et index.ts faut exporter ) : Done
Générer et appliquer les migrations (Tout ce qui manipuler dans le schema de la BDD faut appliquer la migration) : Done
Utiliser direct env. et non process.env. pour les variables d'environnement (JWT_SECRET) : Done

Créer les 2 middlewares: Authenticated, GetUserProfile: Done

Rajouter les access et refresh token pour l'authentification (dans l'endpoint validation du code OTP): Done
        créer une nouvelle table refresh tokens avec l'id du user comme FK: Done
        créer la relation avec le user: Done
        faire des recherches sur le renvoi du token: Done   
                génération(login/register -> token, hashé et inséré en BDD)     
                vérification(dans l'endpoint on vérifie si le hash du token est en BDD si oui access token sinon 401)
                rotation(ancien refresh revoqué)
        modifier dans login et register pour que ça prenne en compte access et refresh token: Done
        création de l'endpoint: Done
        migration: Done

Ajouter une table OTP pour la vérification (avec un endpoint spécifique): Done
Ajouter de nouveaux endpoints pour renvoyer un nouveau code (pas reçu de code clique sur le lien "renvoyer un nouveau code", vérification OTP, refresh token): Done
Tester avec Postman: register,verify-otp,resend-otp,login,me, refresh-token: Done

Remarques:
Revoir les messages: etre plus explicite: Done
modifier l'id pour mettre le préfixe (paramètre ksuid): Done


Endpoint pour CRUD les véhicules (par le chauffeur): faire un update du role du user pour qu'il soit chauffeur s'il est client de base: Done
*/



